function output = blackmanharris_test() %#codegen 
    win=1024;
    % bm=blackmanharris(win);
    % % wvtool(bm);
    
%     spw_j=importdata("test.txt"); 
spw_j = zeros(1, 1024);


    output=periodogram(spw_j,blackmanharris(win),win); % Блэкмана-Харриса
end